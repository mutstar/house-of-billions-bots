# Telegram bot tokens — @BotFather
BOT_TOKEN_DEEPFAKE=
BOT_TOKEN_MBTI=

# Allowlist file — one Telegram @handle per line (without @), comments with #
ALLOWED_HANDLES_FILE=src/data/allowed_handles.txt

# Deepfake bot
QUIZ_DATA_FILE=src/data/quiz_data.json
ATTEMPT_FILE_PATH=/data/attempt_counts.json

# MBTI bot
MBTI_IMAGE_DIR=src/data/mbti_images
# Optional — GCS public URL prefix for X share card preview, e.g. https://storage.googleapis.com/<bucket>/mbti
MBTI_SHARE_IMAGE_URL_BASE=
